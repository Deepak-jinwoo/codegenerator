When updating the project
- Check if the changes affect the `README.md` in `trickle/notes`.
- If features are added or removed, update the "Features" section.
- If the architecture changes, update the "Architecture" section.